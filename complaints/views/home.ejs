<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/styles.css" />
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <title>VIT Complaints</title>
</head>

<body>
    <header class="header">
        <div class="logo" >VIT COMPLAINTS</div>
        <div class="button-container">
            <a href="/" class="home">Home</a>
            <a  class="add-complaint-btn" href="/complaints">Add Complaint</a></li>
        </div>
    </header>
    <section class="section">
    <div class="filter-container" >
        <form method="POST" action="/filter" class="filter">
            <label class="filter-label" for="department">Filter by Department:</label>
            <select name="filterValue" id="department" class="filter-select" onchange="this.form.submit()">
                <option value="all">All</option>
                <option value="cse">CSE</option>
                <option value="it">IT</option>
                <option value="ece">ECE</option>
                <option value="mech">Mechanical</option>
                <option value="eee">EEE</option>
                <option value="civil">Civil</option>
                <option value="csbs">CS&BS</option>
                <option value="aids">AI&DS</option>
                <option value="aiml">AI&ML</option>
                
            </select>

        </form>
    </section>
    </div>

    <div class="card-container">
        <% for (let i=0; i < data.length; i++) { %>
            <% if (data[i]) { %>
                <div class="card">
                    <div class="card-head">
                        <h3>
                            <%= data[i].type %>
                        </h3>
                        <form method="post" action="/delete">
                            <input type="hidden" name="postId" value="<%= data[i]._id %>">
                            <button class="material-symbols-outlined bin" type="submit">delete</button>
                        </form>
                    </div>
                    <p>
                        <%= data[i].complaint %>
                    </p>
                    <div class="btn-like-holder">
                        <a class="submit-button" href="/posts/<%= data[i]._id %>">View More</a>
                        <form method="POST" action="/likes" class="btn-component like-form">
                            <input type="hidden" name="thumbsup" value="<%= data[i]._id %>" />
                            <button type="submit" class="material-symbols-outlined like-btn" title="Like">thumb_up</button>
                            <span class="counter"><%= data[i].likes %></span>
                        </form>
                        
                    </div>
                </div>
                <% } %>
                    <% } %>
    </div>

    <script src="/script.js"></script>
</body>

</html>
